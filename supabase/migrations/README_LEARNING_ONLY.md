# Raahi Learning migration directory

This directory is intentionally reserved for the **Raahi Learning** migration chain only.

Do not restore the older Raahi mobility/ride timestamped migrations here. They remain in repository history/main and belong to a different product/database.

Current controlled chain:

- 0001–0106: Foundation + Identity
- 0200–0203: Locations
- later files only as approved by the Raahi Learning V1.2 implementation gates

Every shared/dev schema change after first application must use a forward-fix migration.