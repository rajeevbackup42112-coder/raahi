RAAHI_LEARNING_DEV_WRITES_ENABLED

This marker is intentionally present during DEV/STAGE.

Pilot cutover rule:
- delete this file in the same reviewed cutover commit that seals/removes automatic
  synthetic writer workflow triggers;
- every mutating DEV workflow must fail closed if this marker is absent;
- never recreate this marker after real pilot user data exists in this Supabase project
  unless the project is first returned to a fully isolated synthetic-only environment.
