name: Raahi Learning Activity Side Effects

on:
  push:
    branches:
      - raahi-learning-implementation-v1
    paths:
      - 'supabase/migrations/1027_v13_activity_submission_side_effects.sql'
      - 'apps/raahi-learning/live-product-fix-v13.js'
      - 'supabase/functions/dev-test-identities/**'
      - 'tests/raahi-learning-e2e/activity-side-effects.mjs'
      - '.github/workflows/raahi-learning-activity-side-effects.yml'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: raahi-learning-activity-side-effects
  cancel-in-progress: true

jobs:
  side-effects:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Guard implementation branch
        run: test "$GITHUB_REF" = "refs/heads/raahi-learning-implementation-v1"
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Install deterministic harness dependencies
        working-directory: tests/raahi-learning-e2e
        run: npm ci --ignore-scripts
      - name: Audit harness dependencies
        working-directory: tests/raahi-learning-e2e
        run: npm audit --audit-level=high
      - name: Install Chromium
        working-directory: tests/raahi-learning-e2e
        run: npx playwright install --with-deps chromium
      - name: Run Activity side-effect proof
        working-directory: tests/raahi-learning-e2e
        run: node activity-side-effects.mjs
      - name: Upload sanitized Activity evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: raahi-learning-activity-side-effects-${{ github.run_id }}-${{ github.run_attempt }}
          path: tests/raahi-learning-e2e/artifacts-sidefx-activity/
          if-no-files-found: warn
          retention-days: 30
