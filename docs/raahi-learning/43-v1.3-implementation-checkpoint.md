# Raahi Learning V1.3 — Implementation Checkpoint

**Status:** V1.3 product/domain design remains frozen; bounded backend/browser delta plus the one-time AI Builder v2 internal retrofit are closed in DEV. The active gate is now real hosted Google/phone Auth proof followed by the mandatory walking skeleton.

Repository: `rajeevbackup42112-coder/raahi`  
Branch: `raahi-learning-implementation-v1`  
Supabase DEV project: `iiwwmqokaeflaenhlyip`

No public deployment has been performed.

## 1. Product decisions preserved

V1.3 does **not** reopen the V1.2 domain model. The following remain authoritative:

- Account is distinct from Learner;
- one active managing guardian per Learner in V1;
- no turning-18 migration/lifecycle;
- no public Learner directory;
- active manager owns formal learner-side marketplace/relationship decisions where a manager exists;
- guardian management never grants Test-taking impersonation;
- Enquiry is the controlled relationship start; Trial remains optional;
- Class Invitation precedes Membership and a valid pending invitation reserves capacity;
- one responsible Teacher per Class in V1;
- no attendance, generic progress percentage, public star ratings/reviews, global Community or unrestricted public DM;
- Ads remain education-only, Sponsored-labelled and excluded from private learning/Class/Test/message surfaces.

The principal V1.3 product amendment remains:

**Google-primary authentication + selective periodic phone trust.**

Google name/photo are editable onboarding defaults only. Phone is trust/contact proof, not the routine login path. Stale phone trust must not remove ordinary existing learning/Test/safety access.

## 2. Applied V1.3 DEV migrations

Applied after the completed V1.2 baseline:

- `1015_v13_contextual_inbox_and_org_teacher_picker`
- `1016_v13_learner_self_access_invitations`
- `1017_v13_organization_member_invitations`
- `1018_v13_notification_transition_wiring`
- `1019_v13_notification_initial_enquiry_dedup`
- `1020_v13_phone_trust_projection`
- `1021_v13_phone_trust_command_guards`

Key contracts now include:

- unified authorized conversation projection;
- narrow eligible Organization responsible-teacher picker;
- private expiring Learner self-access invitation issue/preview/accept/revoke;
- private expiring Organization-member invitation issue/preview/accept/revoke;
- best-effort notifications for important Enquiry/Class/Activity/Test transitions;
- server-derived `unverified / stale / fresh` phone-trust state from Supabase Auth;
- 90-day phone-trust guards on selected creation/escalation commands only.

Private invitation secrets are bearer values returned only when issued, stored hash-only, one-time/expiring, and acceptance rechecks current authority.

## 3. Runtime/security evidence

V1.3 runtime markers include:

- `V13_CONTEXTUAL_INBOX_ORG_PICKER_PASS`
- `V13_LEARNER_SELF_ACCESS_INVITATION_PASS`
- `V13_ORGANIZATION_MEMBER_INVITATION_PASS`
- `V13_NOTIFICATION_TRANSITION_WIRING_PASS`
- `V13_PHONE_TRUST_PROJECTION_PASS`
- `V13_PHONE_TRUST_COMMAND_GUARDS_PASS`

Touched V1.2 Enquiry/Class Communication/Activities/Tests regressions remain green.

Supabase Security Advisor after migration 1021: **0 findings**.

Notification delivery is best-effort and must not roll back the core business transition. Notification bodies do not copy private message content and Sponsored viewer push remains prohibited.

## 4. AI Builder v2 internal retrofit closure

The finite Screen ↔ Backend findings were closed without domain redesign:

- live `teacher-members` renderer;
- correct Community report targeting;
- `community-post` added to canonical route coverage;
- capability-aware Organization workspace controls;
- safe centralized Notification destinations;
- invitation bearer values removed from OAuth redirect queries and retained only through local same-origin handoff;
- stronger route/capability/deep-link/test oracles.

See `45-v1.3-screen-backend-contract-audit.md`, `46-v1.3-side-effects-matrix-audit.md` and `47-ai-builder-v2-internal-retrofit-closure-v1.3.md`.

## 5. Browser evidence after retrofit

- canonical reachable routes: **84**;
- desktop + mobile live contract: **84 × 2 = 168/168 PASS**;
- live-contract issues: **0**;
- route/workspace guard failures: **0**;
- focused V1.3 action contracts: **12/12 PASS**;
- privileged deep-link guards: **32/32 PASS**;
- semantic checks: **15/15 PASS**;
- workspace checks: **154/154 PASS**;
- interaction checks: **26/26 PASS**;
- no direct operational-table browser DML;
- no phone-primary login path in `live.js`;
- no raw Account-UUID user workflow.

Frozen byte anchors remain unchanged:

- `app.fixture.js`: `6eb67b36931c45aa4113c77005d82c13bb14ec145500e08cfd92130ebcef576c`
- `styles.css`: `b2d89e454f8e13712d10058c876f5efe2c8c69acf73dd6aade158241900f7bdd`

Current retrofit anchors:

- `app.live-core-v13.js`: `b8fea9446bba5171aa399d709eede2a0403949e96015157aa0ab1db320834a0f`
- `live.js`: `cdc44f514b1f396799313c977a4a48fe8efad33ae1f6054db776682c977796aa`

## 6. Reproducible source / persistent backup

Canonical GitHub recovery:

```bash
node apps/raahi-learning/build-source-v13.mjs
```

The immutable original V1.3 base tar must hash to:

`9aa71d002775f719970fcf6d48c324f4f3270ac9f65aa1c7e8a2f9f17c2dc9cc`

The verified retrofit patch hashes are:

- compressed patch: `8a68163024ef1b100ebc4d56b8b1cfa56bf3459fc9f897952b6ca5602a19afa9`
- raw patch: `ced0f5ca5b590e8ca344011f8f008c8fa719d57cca6ca3ead63b01a4fc6efc08`

The builder verifies the base, patch and reconstructed file hashes before completing.

Persistent Library backup:

`/Raahi Learning/Raahi_Learning_Integrated_V1.3_DEV.zip`

ZIP SHA-256:

`379dece3fb33fadd68843823a82917469b77e39b8d24448328b713ff98b246ef`

## 7. What is intentionally NOT complete

Do not claim these as verified yet:

1. a real hosted DEV Google OAuth round trip;
2. hosted DEV SMS/test-OTP configuration;
3. a real signed-in Google-primary Account attaching/reverifying phone;
4. observed server-owned phone confirmation evidence changing through that real flow;
5. interruption/resume of a real trust-sensitive command after phone verification;
6. the mandatory real walking-skeleton E2E;
7. the final persona/adversarial real-auth cohort;
8. true concurrency/load/stress/soak, p95/p99, deadlock/connection-pool evidence;
9. production launch-readiness sign-off.

The connected Supabase management capability exposes database/Edge operations but not the hosted Auth provider/test-OTP configuration required to prove those external boundaries. Do not compensate by adding an application fake-OTP bypass.

## 8. Exact next execution order

1. Verify hosted DEV Google provider configuration and perform a real browser OAuth round trip.
2. Prove Google → Supabase session → `bootstrap_account` → same Raahi Account after logout/login → editable profile → server-derived contexts.
3. Verify hosted DEV phone/test-OTP configuration through an authorized Auth-management surface.
4. Prove signed-in phone attach/reverify and observe trusted server-owned confirmation state.
5. Execute the mandatory walking skeleton:
   **Google sign-in → Account/bootstrap → Learner context → discovery → Enquiry → provider engage → Class Invitation → phone interruption/resume → Membership → Class → contextual message.**
6. After R4 passes, implement remaining work only as vertical slices, using the frozen side-effects decisions.
7. Run persona/adversarial E2E, then reliability/security/load/launch gates.

## 9. Change-control rule

A failure must first be classified as Domain, Integration, Implementation, or Test/Harness. Only a genuine Domain defect reopens:

`rules → entities → relationships → states → permissions → UI → tests → architecture/DB`.

Do not deploy implicitly.